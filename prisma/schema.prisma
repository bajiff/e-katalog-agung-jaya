generator client {
  provider = "prisma-client"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Produk {
  id        String   @id
  nama      String
  harga     Int
  kategori  String
  createdAt DateTime @default(now())
}
